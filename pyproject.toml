[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "qaddir"
version = "0.1.0"
description = "AI-assisted preliminary vehicle damage assessment"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
