# Full environment. Equivalent to the previous flat file, now composed from the
# per-area groups so a single area can be installed on its own:
#
#   pip install -r requirements-cv.txt     # data + CV only
#   pip install -r requirements-llm.txt    # data + LLM only
#   pip install -r requirements.txt        # everything
#
# Install the project itself in editable mode so `import qaddir` works:
#
#   pip install -e .
-r requirements-app.txt
