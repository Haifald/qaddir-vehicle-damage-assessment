# Local Data Structure

Place the datasets locally using this structure:

```text
data/
├── cardd/
│   ├── raw/
│   └── processed/
└── carparts/
    ├── raw/
    └── processed/
```

Dataset files are stored locally and are excluded from Git.
