"""Streamlit MVP: upload, inference display and generated report.

The interface calls the shared pipeline rather than reimplementing inference,
so what the demo shows is what the evaluation measured.
"""
